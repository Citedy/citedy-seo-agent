---
description: "Use Citedy MCP tools for SEO content creation, trend scouting, competitor analysis, article generation, social media publishing, video shorts, content ingestion, lead magnets, and warm lead reports"
alwaysApply: false
---

# Citedy SEO Agent — Tool Usage Guide

## MCP Endpoint

```
https://mcp.citedy.com/mcp
```

**Auth**: Bearer token via `CITEDY_API_KEY` environment variable.

**Registration**: If the user does not have an API key, register via:
```
POST https://www.citedy.com/api/agent/register
```

---

## When to Use Citedy Tools

Use Citedy MCP tools whenever the user wants to:
- Research trending topics or competitor content
- Generate SEO-optimized blog articles
- Create social media posts from existing content
- Publish content to connected social accounts
- Produce AI avatar video shorts
- Ingest URLs or documents into structured content
- Create lead magnets (checklists, swipe files, frameworks)
- Create warm lead account reports from company domains
- Manage a content calendar or automate publishing

---

## Tool Groups

### Account & Status

| Tool | When to use |
|------|-------------|
| `agent.me` | Check authenticated user info, credits balance, connected accounts |
| `agent.status` | Get platform health, quota usage, active sessions |
| `agent.health` | Check the agent service health and connectivity |

Always call `agent.me` first if the user asks about their account or available credits.

---

### Trend Scouting

| Tool | When to use |
|------|-------------|
| `scout.x` | Find trending topics, viral posts, and keyword signals on X (Twitter) |
| `scout.reddit` | Discover hot threads and rising discussions on Reddit |

Use these when the user wants to know what to write about next, or to validate a topic idea against current trends.

---

### Competitor Analysis

| Tool | When to use |
|------|-------------|
| `competitors.discover` | Find competitors for a given domain or niche |
| `competitors.scout` | Analyze competitor content strategy, top posts, keywords, and gaps |

Use before generating articles when the user wants to outrank specific domains or understand the competitive landscape.

---

### Content Gap Analysis

| Tool | When to use |
|------|-------------|
| `gaps.generate` | Identify content topics that competitors rank for but the user's site does not |
| `gaps.list` | List previously generated content gaps |

Run after `competitors.scout` to get a prioritized list of high-opportunity topics.

---

### Warm Lead Reports

| Tool | When to use |
|------|-------------|
| `citedy_warm_lead.start` | Start a warm lead report for one company domain |
| `citedy_warm_lead.get` | Poll status and read the canonical JSON result |
| `citedy_warm_lead.export` | Export a completed report as JSON, CSV, or Excel |

Use this when the user asks who to contact at a target account, why now, or what a grounded first message should say.

V1 does not send email automatically. Treat drafts as review-required and tied to evidence.

---

### Article Generation & Management

| Tool | When to use |
|------|-------------|
| `article.generate` | Generate a full SEO article from a keyword, topic, or brief |
| `article.get` | Get a single article by id (poll draft status, check result) |
| `article.list` | List existing articles for the tenant |
| `article.publish` | Publish a draft article (status: generated → published) |
| `article.unpublish` | Unpublish a published article (→ draft) |
| `article.delete` | Permanently delete an article |

Key options for `article.generate`:
- `keyword` — target search term
- `language` — one of 55 supported languages
- `persona` — writing voice/style
- `illustrations` — include AI-generated images
- `voiceover` — include TTS audio
- `internalLinks` — auto-link to existing content
- `auto_publish` — when `false`, article stays as draft for review before publishing

**Important**: `article.generate` is synchronous — it returns the full article directly (timeout up to 5 minutes). No polling needed.

**Draft mode**: Set `auto_publish: false` to generate articles as drafts. Review them, then use `article.publish` to publish. Useful for regulated industries (healthcare, finance) requiring content review before publishing.

---

### Social Media Adaptations

| Tool | When to use |
|------|-------------|
| `adapt.generate` | Convert a blog article into platform-optimized social posts |

Supported platforms: X, LinkedIn, Facebook, Reddit, Instagram, TikTok, YouTube, Shopify, Telegram.

Pass the article ID or raw text plus a list of target platforms.

---

### Social Publishing

| Tool | When to use |
|------|-------------|
| `social.publish` | Push a generated adaptation to a connected social account |

Requires a connected account (user must authorize via Citedy dashboard). Check connected accounts with `agent.me`.

---

### AI Video Shorts

| Tool | When to use |
|------|-------------|
| `shorts.script` | Generate a video script from an article or topic |
| `shorts.generate` | Produce an AI avatar video from a script |
| `shorts.get` | Poll async video generation status |
| `shorts.avatar` | Select or customize the AI avatar |
| `shorts.merge` | Merge video segments and add subtitles |

Typical flow: `shorts.script` → `shorts.generate` → `shorts.merge`.

`shorts.generate` is async — poll `shorts.get(id)` until `status === "completed"`.

---

### Content Ingestion

| Tool | When to use |
|------|-------------|
| `ingest.create` | Ingest a single URL (web page, YouTube video, PDF) into structured content |
| `ingest.batch` | Ingest multiple URLs in one request |
| `ingest.get` | Poll ingestion job status |
| `ingest.content.get` | Retrieve structured content after ingestion |

After ingestion, the content can be passed directly to `article.generate` as source material.

---

### Brand Scanning

| Tool | When to use |
|------|-------------|
| `brand.scan` | Scan LLM responses for brand mentions and visibility |
| `brand.scan.get` | Poll brand scan results (async companion) |

`brand.scan` is async — poll `brand.scan.get(id)` until complete.

---

### Lead Magnets

| Tool | When to use |
|------|-------------|
| `leadmagnet.generate` | Create a downloadable lead magnet (checklist, swipe file, or framework) from a topic or article |
| `leadmagnet.get` | Poll lead magnet generation status |
| `leadmagnet.publish` | Publish a generated lead magnet |
| `leadmagnet.archive` | Archive a lead magnet |

Returns a structured document ready for publishing or gating behind an email opt-in form.

---

### Products / Knowledge Base

| Tool | When to use |
|------|-------------|
| `products.list` | List products in the user's knowledge base |
| `products.create` | Add a new product to the knowledge base |
| `products.delete` | Remove a product from the knowledge base |
| `products.search` | Search products by query |

The knowledge base is used to ground article generation in accurate product descriptions and brand voice.

---

### SEO Analysis

| Tool | When to use |
|------|-------------|
| `seo.meta_tags.check` | Analyze meta tags for SEO best practices |
| `seo.headings.check` | Check heading structure (H1-H6) |
| `seo.links.analyze` | Analyze external and internal links |
| `seo.internal_links.plan` | Plan internal linking strategy |
| `seo.og.preview` | Preview Open Graph social cards |
| `seo.schema.validate` | Validate Schema.org structured data |
| `seo.robots.check` | Check robots.txt rules |
| `seo.sitemap.check` | Validate sitemap structure |
| `seo.og_image.generate` | Generate Open Graph images |

---

### Settings & Personas

| Tool | When to use |
|------|-------------|
| `settings.get` | Get current agent configuration and preferences |
| `personas.list` | List available writing personas/voices for article generation |

Use `personas.list` to discover available writing styles before calling `article.generate` with a `persona` parameter.

---

### Content Calendar

| Tool | When to use |
|------|-------------|
| `schedule.gaps` | View content gaps mapped to the publishing calendar |
| `schedule.list` | List scheduled content items |
| `schedule.suggest` | Get optimal time slot recommendations for publishing |

---

### Autopilot Sessions

| Tool | When to use |
|------|-------------|
| `session.create` | Start a fully automated content autopilot session |

A session runs the full pipeline: scout → gaps → generate → adapt → publish, on a recurring schedule.

---

### Webhooks

| Tool | When to use |
|------|-------------|
| `webhooks.list` | List registered webhook endpoints |
| `webhooks.register` | Register a URL to receive event notifications |
| `webhooks.deliveries` | View webhook delivery history |
| `webhooks.delete` | Remove a webhook |

Events include: `article.generated`, `article.published`, `article.unpublished`, `article.deleted`, `article.failed`, `ingestion.completed`, `social_adaptation.generated`, `lead_magnet.ready`, `scout.results_ready`, `session.articles_generated`, `billing.credits_low`, `billing.credits_empty`.

---

## Async Polling Pattern

Many Citedy tools are async. The standard pattern is:

```
1. Call async tool (e.g. scout.x, shorts.generate, ingest.create, ingest.batch, leadmagnet.generate, brand.scan)
2. Poll companion tool (scout.x.result, shorts.get, ingest.get, ingest.get, leadmagnet.get, brand.scan.get) every few seconds
3. When status === "completed", retrieve the result
```

Note: `article.generate` is synchronous — no polling needed.

Let the user know the job has started and provide updates as they arrive.
